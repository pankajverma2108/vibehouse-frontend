
  # Extend Base Design Sections

  This is a code bundle for Extend Base Design Sections. The original project is available at https://www.figma.com/design/qoq8ut5ibyTuOBvnSBYQKd/Extend-Base-Design-Sections.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  